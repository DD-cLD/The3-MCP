---
id: phasers-via-measures-curriculum
title: "Teach phasers through measures — musical alignment and landing effects inside the measure count, manual-read vs live-tested"
role: operational-meta
tags: [ma3, doctrine, phasers, curriculum, v2.4]
when_to_load: "Before a phaser-learning or drilling session — this is Dave's prep method: read the manual's measures material, then test it against live behavior before building real phasers"
status: active
source: "findings/INBOX.md, 2026-07-15, Dave dictated live"
verified: grandMA3 onPC 2.4.2.2 (Mac)
supersedes: []
superseded_by: null
---

Dave's stated curriculum for how phasers should be learned and drilled going forward: think about phasers through **measures** — musical alignment, and specifically how effects land inside a measure count — rather than treating phaser timing as an abstract numeric setting.

Prep method, in order: (1) read the manual's material on measures first; (2) compose small test cases comparing what the manual describes against what the console actually does live — warranted since the manual has already been caught wrong multiple times this session (sub-range Thru syntax, missing Store Default entry); (3) once that foundation holds, move to steps/timing; (4) then progress to multi-step and more complex phasers.

This is named as the next phaser session's curriculum, not something drilled tonight — it sets up structure for that session rather than reporting a result from this one.

See `v24-phaser-model` for the existing foundational fact set (phasers are recipes, CLI ranges are spatial not temporal, Shapes pool) this curriculum will test against and build on.

**Executed 2026-07-16** — the following phaser session ran this exact prep method: the manual's measures material was read first (surfacing repo-manual gap #4 — no Measure definition in the repo manual at all), then tested against forum-sourced ground truth and live console behavior. Result banked as `measure-layer-math` (the definition + runtime-scale formula) and the full study, `PHASER_MEASURES_STUDY_v0.1.md` (15-test manual-vs-live matrix).

History: dictated live 2026-07-15 as prep for the next phaser session. **2026-07-16:** curriculum executed as planned — see `measure-layer-math` for the resulting fact set.
